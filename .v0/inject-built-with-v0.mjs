// Vercel/v0 build hook placeholder.
// The production build command runs this file before `next build`.
// Keeping it as a no-op allows deployments to proceed when no v0 injection is needed.

console.log('v0 build hook: no injection required')
