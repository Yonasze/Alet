import Link from 'next/link'
import {ArrowRight,Building2} from 'lucide-react'
import {Badge} from '@/components/ui/badge'
import {Button} from '@/components/ui/button'
import {Card,CardContent,CardDescription,CardHeader,CardTitle} from '@/components/ui/card'
import {constructionLabel,getConstructionWorkspace} from '@/services/construction/supabase-construction-service'
import {ConstructionNav} from '../construction-nav'
export default async function ConstructionProjectsPage(){const d=await getConstructionWorkspace();return <div className="space-y-6"><div><p className="text-sm font-medium text-primary">Construction</p><h2 className="mt-1 font-serif text-3xl font-semibold">Project execution</h2><p className="mt-2 text-sm text-muted-foreground">One construction workspace for each Project Module building.</p></div><ConstructionNav/><div className="grid gap-4 lg:grid-cols-2">{d.projects.map(p=>{const active=d.packages.filter(w=>w.project_id===p.id&&['active','delayed','blocked','awaiting_inspection'].includes(w.status)).length;return <Card key={p.id}><CardHeader><div className="flex justify-between"><div><CardTitle>{p.name}</CardTitle><CardDescription>{constructionLabel(p.phase)}</CardDescription></div><Badge>{p.floors_completed} of {p.total_floors}</Badge></div></CardHeader><CardContent className="space-y-4"><div className="grid grid-cols-2 gap-3"><div className="rounded-lg border p-3"><p className="text-xs text-muted-foreground">Active work</p><p className="text-2xl font-semibold">{active}</p></div><div className="rounded-lg border p-3"><p className="text-xs text-muted-foreground">Open issues</p><p className="text-2xl font-semibold">{d.issues.filter(i=>i.project_id===p.id&&!['closed','cancelled'].includes(i.status)).length}</p></div></div><Button asChild className="w-full"><Link href={`/erp/construction/projects/${p.id}`}>Open construction workspace<ArrowRight className="size-4"/></Link></Button></CardContent></Card>})}{d.projects.length===0?<Card><CardContent className="py-14 text-center"><Building2 className="mx-auto size-9"/><p>No projects available.</p></CardContent></Card>:null}</div></div>}
