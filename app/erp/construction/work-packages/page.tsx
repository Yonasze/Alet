import {Badge} from '@/components/ui/badge'
import {Card,CardContent,CardDescription,CardHeader,CardTitle} from '@/components/ui/card'
import {constructionLabel,getConstructionWorkspace} from '@/services/construction/supabase-construction-service'
import {ConstructionNav} from '../construction-nav'
import {PackageUpdateForm,WorkPackageForm} from '../construction-forms'
export default async function WorkPackagesPage(){const d=await getConstructionWorkspace();const floors=new Map(d.floors.map(f=>[f.id,f]));return <div className="space-y-6"><div><p className="text-sm font-medium text-primary">Construction</p><h2 className="mt-1 font-serif text-3xl font-semibold">Work packages</h2><p className="mt-2 text-sm text-muted-foreground">Plan, assign, update and inspect construction work.</p></div><ConstructionNav/><div className="grid gap-6 xl:grid-cols-[.8fr_1.2fr]"><Card><CardHeader><CardTitle>New work package</CardTitle></CardHeader><CardContent><WorkPackageForm projects={d.projects} floors={d.floors} profiles={d.profiles}/></CardContent></Card><div className="space-y-4">{d.packages.map(w=><Card key={w.id}><CardHeader><div className="flex justify-between gap-3"><div><CardTitle>{w.code} · {w.title}</CardTitle><CardDescription>{constructionLabel(w.category)} · {w.floor_id?(floors.get(w.floor_id)?.name??`Floor ${floors.get(w.floor_id)?.floor_number}`):'Whole project'}</CardDescription></div><div className="flex gap-2"><Badge variant="outline">{w.progress_percent}%</Badge><Badge>{constructionLabel(w.status)}</Badge></div></div></CardHeader><CardContent><PackageUpdateForm w={w} profiles={d.profiles}/></CardContent></Card>)}{d.packages.length===0?<Card><CardContent className="py-14 text-center text-sm text-muted-foreground">Create the first work package.</CardContent></Card>:null}</div></div></div>}
