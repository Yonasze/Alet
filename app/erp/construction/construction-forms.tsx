'use client'
import {useActionState,useState} from 'react'
import {Button} from '@/components/ui/button'
import type{ConstructionFloor,ConstructionProfile,ConstructionProject,WorkPackage}from '@/services/construction/supabase-construction-service'
import{createDailyReportAction,createInspectionAction,createIssueAction,createMilestoneAction,createWorkPackageAction,type ConstructionActionState,updateWorkPackageAction}from './actions'
const initial:ConstructionActionState={}
const field='h-10 w-full rounded-lg border border-input bg-background px-3 text-sm outline-none focus:border-ring focus:ring-2 focus:ring-ring/30'
const area='min-h-24 w-full rounded-lg border border-input bg-background px-3 py-2 text-sm outline-none focus:border-ring focus:ring-2 focus:ring-ring/30'
function floorLabel(f:Pick<ConstructionFloor,'floor_number'|'name'>){if(f.name)return f.name;if(f.floor_number<0)return `Basement ${Math.abs(f.floor_number)}`;if(f.floor_number===0)return 'Ground Floor';return `Floor ${f.floor_number}`}
function Message({s}:{s:ConstructionActionState}){return s.error?<p className="rounded-lg bg-destructive/10 p-3 text-sm text-destructive">{s.error}</p>:s.success?<p className="rounded-lg bg-emerald-50 p-3 text-sm text-emerald-800">{s.success}</p>:null}
export function WorkPackageForm({projects,floors,profiles}:{projects:ConstructionProject[];floors:ConstructionFloor[];profiles:ConstructionProfile[]}){const[s,a]=useActionState(createWorkPackageAction,initial);const[pid,setPid]=useState(projects[0]?.id??'');return <form action={a} className="space-y-4"><Message s={s}/><div className="grid gap-4 md:grid-cols-2"><label className="space-y-1 text-sm font-medium">Project<select name="project_id" value={pid} onChange={e=>setPid(e.target.value)} className={field}>{projects.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label className="space-y-1 text-sm font-medium">Floor<select name="floor_id" className={field}><option value="">Whole project</option>{floors.filter(f=>f.project_id===pid).map(f=><option key={f.id} value={f.id}>{floorLabel(f)}</option>)}</select></label><label className="space-y-1 text-sm font-medium">Code<input name="code" className={field} placeholder="WP-STRUCT-01"/></label><label className="space-y-1 text-sm font-medium">Title<input name="title" className={field} required/></label><label className="space-y-1 text-sm font-medium">Category<select name="category" className={field}>{['site_preparation','excavation','foundation','structure','floor_construction','masonry','electrical','plumbing','fire_protection','elevator','finishing','external_works','testing_commissioning','other'].map(x=><option key={x} value={x}>{x.replaceAll('_',' ')}</option>)}</select></label><label className="space-y-1 text-sm font-medium">Priority<select name="priority" className={field}><option>normal</option><option>high</option><option>critical</option><option>low</option></select></label><label className="space-y-1 text-sm font-medium">Planned start<input type="date" name="planned_start" className={field}/></label><label className="space-y-1 text-sm font-medium">Planned finish<input type="date" name="planned_finish" className={field}/></label><label className="space-y-1 text-sm font-medium">Responsible person<select name="assigned_to" className={field}><option value="">Unassigned</option>{profiles.map(p=><option key={p.id} value={p.id}>{p.full_name}</option>)}</select></label><label className="space-y-1 text-sm font-medium">Contractor<input name="contractor_name" className={field}/></label></div><label className="block space-y-1 text-sm font-medium">Description<textarea name="description" className={area}/></label><label className="block space-y-1 text-sm font-medium">Dependencies<textarea name="dependencies" className={area}/></label><label className="block space-y-1 text-sm font-medium">Public-safe summary<textarea name="public_summary" className={area}/></label><label className="flex gap-2 text-sm"><input type="checkbox" name="inspection_required" defaultChecked/>Inspection required</label><input type="hidden" name="progress_percent" value="0"/><Button type="submit">Create work package</Button></form>}
export function PackageUpdateForm({w,profiles}:{w:WorkPackage;profiles:ConstructionProfile[]}){const bound=updateWorkPackageAction.bind(null,w.id,w.project_id);const[s,a]=useActionState(bound,initial);return <form action={a} className="space-y-3"><Message s={s}/><div className="grid gap-3 md:grid-cols-2"><label className="space-y-1 text-xs font-medium">Title<input name="title" defaultValue={w.title} className={field}/></label><label className="space-y-1 text-xs font-medium">Status<select name="status" defaultValue={w.status} className={field}>{['planned','active','awaiting_inspection','approved','completed','delayed','blocked','cancelled'].map(x=><option key={x}>{x}</option>)}</select></label><label className="space-y-1 text-xs font-medium">Progress %<input name="progress_percent" type="number" min="0" max="100" step="0.01" defaultValue={w.progress_percent} className={field}/></label><label className="space-y-1 text-xs font-medium">Priority<select name="priority" defaultValue={w.priority} className={field}><option>low</option><option>normal</option><option>high</option><option>critical</option></select></label><label className="space-y-1 text-xs font-medium">Assigned<select name="assigned_to" defaultValue={w.assigned_to??''} className={field}><option value="">Unassigned</option>{profiles.map(p=><option key={p.id} value={p.id}>{p.full_name}</option>)}</select></label><label className="space-y-1 text-xs font-medium">Contractor<input name="contractor_name" defaultValue={w.contractor_name??''} className={field}/></label></div><label className="block space-y-1 text-xs font-medium">Description<textarea name="description" defaultValue={w.description??''} className={area}/></label><input type="hidden" name="planned_start" value={w.planned_start??''}/><input type="hidden" name="planned_finish" value={w.planned_finish??''}/><input type="hidden" name="dependencies" value={w.dependencies??''}/><input type="hidden" name="public_summary" value={w.public_summary??''}/><Button type="submit" variant="outline">Save work package</Button></form>}
export function DailyReportForm({projects}:{projects:ConstructionProject[]}){const[s,a]=useActionState(createDailyReportAction,initial);return <form action={a} className="space-y-4"><Message s={s}/><div className="grid gap-4 md:grid-cols-3"><label className="space-y-1 text-sm font-medium">Project<select name="project_id" className={field}>{projects.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label className="space-y-1 text-sm font-medium">Date<input name="report_date" type="date" className={field} required/></label><label className="space-y-1 text-sm font-medium">Weather<input name="weather" className={field}/></label><label className="space-y-1 text-sm font-medium">Workforce<input name="workforce_count" type="number" min="0" className={field}/></label></div>{[['work_performed','Work performed'],['equipment_used','Equipment used'],['materials_used','Materials used'],['delays','Delays'],['safety_incidents','Safety incidents'],['site_instructions','Site instructions'],['next_day_plan','Next-day plan']].map(([n,l])=><label key={n} className="block space-y-1 text-sm font-medium">{l}<textarea name={n} className={area} required={n==='work_performed'}/></label>)}<Button type="submit">Save draft report</Button></form>}
export function InspectionForm({packages,profiles}:{packages:WorkPackage[];profiles:ConstructionProfile[]}){const[s,a]=useActionState(createInspectionAction,initial);return <form action={a} className="space-y-4"><Message s={s}/><label className="block space-y-1 text-sm font-medium">Work package<select name="work_package_id" className={field}>{packages.filter(w=>!['approved','completed','cancelled'].includes(w.status)).map(w=><option key={w.id} value={w.id}>{w.code} · {w.title}</option>)}</select></label><div className="grid gap-4 md:grid-cols-2"><label className="space-y-1 text-sm font-medium">Inspection type<input name="inspection_type" defaultValue="Quality inspection" className={field}/></label><label className="space-y-1 text-sm font-medium">Inspector<select name="inspector_id" className={field}><option value="">Assign later</option>{profiles.map(p=><option key={p.id} value={p.id}>{p.full_name}</option>)}</select></label><label className="space-y-1 text-sm font-medium">Scheduled<input name="scheduled_at" type="datetime-local" className={field}/></label></div><label className="block space-y-1 text-sm font-medium">Request comments<textarea name="comments" className={area}/></label><Button type="submit">Request inspection</Button></form>}
export function IssueForm({projects,floors,packages,profiles}:{projects:ConstructionProject[];floors:ConstructionFloor[];packages:WorkPackage[];profiles:ConstructionProfile[]}){const[s,a]=useActionState(createIssueAction,initial);const[pid,setPid]=useState(projects[0]?.id??'');return <form action={a} className="space-y-4"><Message s={s}/><div className="grid gap-4 md:grid-cols-2"><label className="space-y-1 text-sm font-medium">Project<select name="project_id" value={pid} onChange={e=>setPid(e.target.value)} className={field}>{projects.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label className="space-y-1 text-sm font-medium">Work package<select name="work_package_id" className={field}><option value="">General issue</option>{packages.filter(w=>w.project_id===pid).map(w=><option key={w.id} value={w.id}>{w.code} · {w.title}</option>)}</select></label><label className="space-y-1 text-sm font-medium">Floor<select name="floor_id" className={field}><option value="">No floor</option>{floors.filter(f=>f.project_id===pid).map(f=><option key={f.id} value={f.id}>{floorLabel(f)}</option>)}</select></label><label className="space-y-1 text-sm font-medium">Category<select name="category" className={field}>{['design','material_shortage','contractor_delay','quality','safety','weather','access','client_change','other'].map(x=><option key={x}>{x}</option>)}</select></label><label className="space-y-1 text-sm font-medium">Priority<select name="priority" className={field}><option>normal</option><option>high</option><option>critical</option><option>low</option></select></label><label className="space-y-1 text-sm font-medium">Owner<select name="owner_id" className={field}><option value="">Unassigned</option>{profiles.map(p=><option key={p.id} value={p.id}>{p.full_name}</option>)}</select></label><label className="space-y-1 text-sm font-medium">Due date<input name="due_date" type="date" className={field}/></label></div><label className="block space-y-1 text-sm font-medium">Title<input name="title" className={field} required/></label><label className="block space-y-1 text-sm font-medium">Description<textarea name="description" className={area} required/></label><Button type="submit">Record issue</Button></form>}
export function MilestoneForm({projects,floors}:{projects:ConstructionProject[];floors:ConstructionFloor[]}){const[s,a]=useActionState(createMilestoneAction,initial);const[pid,setPid]=useState(projects[0]?.id??'');return <form action={a} className="space-y-4"><Message s={s}/><div className="grid gap-4 md:grid-cols-2"><label className="space-y-1 text-sm font-medium">Project<select name="project_id" value={pid} onChange={e=>setPid(e.target.value)} className={field}>{projects.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label className="space-y-1 text-sm font-medium">Code<input name="code" className={field}/></label><label className="space-y-1 text-sm font-medium">Title<input name="title" className={field} required/></label><label className="space-y-1 text-sm font-medium">Phase<select name="phase" className={field}>{['planning_design','groundbreaking','foundation_work','floor_construction','ready_to_handover','completed','closed'].map(x=><option key={x}>{x}</option>)}</select></label><label className="space-y-1 text-sm font-medium">Target building level<select name="target_floor_number" className={field}><option value="">No specific level</option>{floors.filter(f=>f.project_id===pid).map(f=><option key={f.id} value={f.floor_number}>{floorLabel(f)}</option>)}</select></label><label className="space-y-1 text-sm font-medium">Planned date<input name="planned_date" type="date" className={field}/></label></div><label className="block space-y-1 text-sm font-medium">Internal description<textarea name="description" className={area}/></label><label className="block space-y-1 text-sm font-medium">Public-safe summary<textarea name="public_summary" className={area}/></label><div className="flex flex-wrap gap-4 text-sm"><label className="flex gap-2"><input type="checkbox" name="finance_trigger"/>Trigger matching Finance installment</label><label className="flex gap-2"><input type="checkbox" name="public_on_approval"/>Publish after approval</label></div><input type="hidden" name="progress_percent" value="0"/><Button type="submit">Create milestone</Button></form>}
