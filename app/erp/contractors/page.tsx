import Link from'next/link'
import{AlertTriangle,BadgeCheck,Building,ClipboardCheck,FileSignature}from'lucide-react'
import{Badge}from'@/components/ui/badge'
import{Button}from'@/components/ui/button'
import{Card,CardContent,CardDescription,CardHeader,CardTitle}from'@/components/ui/card'
import{contractorDate,contractorEtb,contractorLabel,getContractorWorkspace}from'@/services/contractors/supabase-contractor-service'
import{ContractorNav}from'./contractor-nav'
export default async function ContractorsPage(){const d=await getContractorWorkspace();const contractors=new Map(d.contractors.map(c=>[c.id,c]));const active=d.contracts.filter(c=>c.status==='active');const pending=d.contractors.filter(c=>c.status==='submitted');const claimQueue=d.claims.filter(c=>['submitted','certified','payment_requested'].includes(c.status));const expiring=d.compliance.filter(x=>['expiring','expired'].includes(x.status));const committed=active.reduce((n,c)=>n+Number(c.contract_value_etb),0);const metrics=[{label:'Approved contractors',value:d.contractors.filter(c=>c.status==='approved').length,detail:`${pending.length} awaiting prequalification`,icon:Building},{label:'Active contracts',value:active.length,detail:contractorEtb(committed)+' active value',icon:FileSignature},{label:'Claim queue',value:claimQueue.length,detail:'Certification and Finance handoff',icon:ClipboardCheck},{label:'Compliance alerts',value:expiring.length,detail:'Expiring or expired records',icon:AlertTriangle}];return <div className="space-y-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-sm font-medium text-primary">Contractors Module</p><h2 className="mt-1 font-serif text-3xl font-semibold">Contractor control center</h2><p className="mt-2 text-sm text-muted-foreground">Prequalification, project appointments, progress claims and performance oversight.</p></div><Button asChild><Link href="/erp/contractors/directory">Add contractor</Link></Button></div><ContractorNav/><section className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{metrics.map(m=><Card key={m.label}><CardHeader className="flex flex-row items-start justify-between"><div><CardDescription>{m.label}</CardDescription><CardTitle className="mt-2 text-3xl">{m.value}</CardTitle></div><span className="rounded-lg bg-primary/10 p-2 text-primary"><m.icon className="size-5"/></span></CardHeader><CardContent className="text-sm text-muted-foreground">{m.detail}</CardContent></Card>)}</section><div className="grid gap-6 xl:grid-cols-[1.2fr_.8fr]"><Card><CardHeader><CardTitle>Active appointments</CardTitle><CardDescription>Current contractor commitments by project.</CardDescription></CardHeader><CardContent className="space-y-3">{active.slice(0,8).map(c=><Link key={c.id} href="/erp/contractors/contracts" className="grid gap-2 rounded-lg border p-3 hover:border-primary md:grid-cols-[1fr_auto_auto] md:items-center"><div><p className="font-medium">{c.contract_number} · {c.title}</p><p className="text-xs text-muted-foreground">{contractors.get(c.contractor_id)?.legal_name} · Ends {contractorDate(c.end_date)}</p></div><p className="font-semibold">{contractorEtb(c.contract_value_etb)}</p><Badge>{contractorLabel(c.status)}</Badge></Link>)}{active.length===0?<div className="py-12 text-center text-sm text-muted-foreground">No active contractor contracts yet.</div>:null}</CardContent></Card><Card><CardHeader><CardTitle>Attention needed</CardTitle></CardHeader><CardContent className="space-y-3"><Link href="/erp/contractors/directory" className="flex items-center justify-between rounded-lg border p-3 hover:border-primary"><span className="text-sm font-medium">Prequalification approvals</span><Badge>{pending.length}</Badge></Link><Link href="/erp/contractors/claims" className="flex items-center justify-between rounded-lg border p-3 hover:border-primary"><span className="text-sm font-medium">Progress claims</span><Badge>{claimQueue.length}</Badge></Link><Link href="/erp/contractors/compliance" className="flex items-center justify-between rounded-lg border p-3 hover:border-primary"><span className="text-sm font-medium">Compliance alerts</span><Badge>{expiring.length}</Badge></Link><Link href="/erp/contractors/evaluations" className="flex items-center justify-between rounded-lg border p-3 hover:border-primary"><span className="flex items-center gap-2 text-sm font-medium"><BadgeCheck className="size-4"/>Evaluations</span><Badge>{d.evaluations.length}</Badge></Link></CardContent></Card></div></div>}
