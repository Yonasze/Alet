import{Badge}from'@/components/ui/badge'
import{Card,CardContent,CardDescription,CardHeader,CardTitle}from'@/components/ui/card'
import{EvaluationForm}from'../contractor-forms'
import{ContractorNav}from'../contractor-nav'
import{contractorDate,contractorLabel,getContractorWorkspace}from'@/services/contractors/supabase-contractor-service'
export default async function EvaluationsPage(){const d=await getContractorWorkspace();const contractors=new Map(d.contractors.map(c=>[c.id,c]));const projects=new Map(d.projects.map(p=>[p.id,p]));return <div className="space-y-6"><div><p className="text-sm font-medium text-primary">Contractors</p><h2 className="mt-1 font-serif text-3xl font-semibold">Performance evaluations</h2><p className="mt-2 text-sm text-muted-foreground">Quality, schedule, safety and commercial performance.</p></div><ContractorNav/><Card><CardHeader><CardTitle>Record evaluation</CardTitle><CardDescription>Scores use a five-point scale and combine into an overall rating.</CardDescription></CardHeader><CardContent><EvaluationForm projects={d.projects} contractors={d.contractors} contracts={d.contracts}/></CardContent></Card><div className="grid gap-4 md:grid-cols-2">{d.evaluations.map(x=><Card key={x.id}><CardHeader><div className="flex items-start justify-between gap-3"><div><CardTitle>{contractors.get(x.contractor_id)?.legal_name}</CardTitle><CardDescription>{projects.get(x.project_id)?.name} · {contractorDate(x.evaluation_date)}</CardDescription></div><Badge>{Number(x.overall_score).toFixed(1)} / 5</Badge></div></CardHeader><CardContent className="space-y-3"><div className="grid grid-cols-2 gap-3 text-sm sm:grid-cols-4"><p>Quality <strong>{x.quality_score}</strong></p><p>Schedule <strong>{x.schedule_score}</strong></p><p>Safety <strong>{x.safety_score}</strong></p><p>Commercial <strong>{x.commercial_score}</strong></p></div><p className="text-sm"><span className="text-muted-foreground">Recommendation:</span> {contractorLabel(x.recommendation)}</p>{x.strengths?<p className="text-sm"><span className="text-muted-foreground">Strengths:</span> {x.strengths}</p>:null}{x.concerns?<p className="text-sm"><span className="text-muted-foreground">Concerns:</span> {x.concerns}</p>:null}</CardContent></Card>)}{d.evaluations.length===0?<Card><CardContent className="py-14 text-center text-sm text-muted-foreground">No contractor evaluations yet.</CardContent></Card>:null}</div></div>}
